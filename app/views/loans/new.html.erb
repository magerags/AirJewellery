<div class="form-show">
        <%= simple_form_for([ @jewellery, @loan ]) do |form| %>
         <h2 style ="color:pink;"><%= form.input :from %></h2>
          <h2 style ="color:pink;"><%= form.input :to %></h2>
          <%= form.button :submit, class: "btn-success" %>
        <% end %>

</div>

<%= link_to "Go back", jewellery_path(@jewellery)%>
